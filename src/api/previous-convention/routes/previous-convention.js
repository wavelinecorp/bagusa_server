'use strict';

/**
 * previous-convention router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::previous-convention.previous-convention');
