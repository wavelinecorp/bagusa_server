'use strict';

/**
 * previous-convention controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::previous-convention.previous-convention');
